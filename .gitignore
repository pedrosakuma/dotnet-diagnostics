# Build artifacts
bin/
obj/
out/
*.user
*.suo
.vs/
.vscode/
.idea/

# Test results
TestResults/
coverage/
*.coverage
*.coveragexml

# Diagnostic artifacts (dumps, traces)
*.nettrace
*.gcdump
*.dmp
artifacts/

# linux-crash-repro: built LD_PRELOAD/LD_AUDIT shims + their capture outputs (CI-only)
tools/linux-crash-repro/*.so
mmaptrace.log
mmaptrace.log.gz
audittrace.log
audittrace.log.gz

# BenchmarkDotNet output (results, logs, captured diagnostics)
BenchmarkDotNet.Artifacts/

# OS
.DS_Store
Thumbs.db

# Tools
.dotnet/

# Local env
.env
.env.local

# Bicep build artifacts (compiled ARM templates)
deploy/azure/**/main.json
nupkgs/
.venv-cfnlint/
.commit-msg.txt

# Transient PR / issue body scratch files (use --body-file with these names)
/pr-body.md
/pr-body*.md
/issue-body*.md
